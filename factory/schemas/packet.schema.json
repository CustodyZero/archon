{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://archon.custodyzero.dev/schemas/packet.schema.json",
  "title": "Archon Factory Packet",
  "description": "A scoped unit of work in the Archon factory system.",
  "type": "object",
  "required": [
    "id",
    "title",
    "intent",
    "change_class",
    "scope",
    "owner",
    "created_at"
  ],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case unique identifier. Must match the filename (without .json)."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable one-line summary."
    },
    "intent": {
      "type": "string",
      "minLength": 1,
      "description": "What is changing and why. Must be explicit."
    },
    "change_class": {
      "type": "string",
      "enum": ["trivial", "local", "cross_cutting", "architectural"],
      "description": "Determines acceptance path. Maps to CLAUDE.md section 6.1 change classes."
    },
    "scope": {
      "type": "object",
      "required": ["packages"],
      "properties": {
        "packages": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Which Archon packages are affected."
        },
        "files_hint": {
          "type": ["array", "null"],
          "items": { "type": "string" },
          "description": "Expected file paths (informational, not enforced)."
        },
        "phase_ref": {
          "type": ["string", "null"],
          "description": "Reference to a phase (e.g. 'P8.1') if applicable."
        }
      },
      "additionalProperties": false
    },
    "owner": {
      "type": "string",
      "minLength": 1,
      "description": "Who is responsible for this work unit."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 creation timestamp."
    },
    "started_at": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "ISO 8601 timestamp when work began. Null if not started."
    },
    "dependencies": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Packet IDs that must be completed/accepted before this packet can start."
    },
    "environment_dependencies": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Dependency keys that must have matching evidence records."
    },
    "status": {
      "type": ["string", "null"],
      "enum": [null, "abandoned", "deferred"],
      "description": "Explicit lifecycle override. Abandoned/deferred packets are exempt from FI-6 and FI-7."
    },
    "feature_id": {
      "type": ["string", "null"],
      "description": "ID of the parent feature, if this packet belongs to a feature."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Freeform tags for filtering."
    }
  },
  "additionalProperties": false
}
