export function LoadingIndicator() {
  return (
    <div
      style={{
        color: 'var(--text-dim)',
        textAlign: 'center',
        padding: '24px',
        fontSize: 12,
      }}
    >
      Loading…
    </div>
  );
}
